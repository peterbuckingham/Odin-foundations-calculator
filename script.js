let add = (a, b) => a + b;
let subtract = (a, b) => a - b;
let divide = (a, b) => a / b;
let multiply = (a, b) => a * b;

//Operators Displayed
let firstNum = 0;
let secondNum = 0;
let operator = '';

function operate(numOne, operators, numTwo) {
    return add(numOne, numTwo);
}